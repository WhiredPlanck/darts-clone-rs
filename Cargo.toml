[package]
name = "darts-clone-rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libdarts-sys = { path = "libdarts-sys", version = "0.1.0" }

[dev-dependencies]
rand = "0.8"
